<section class="index container">
    <div class="post-list">
<% page.posts.forEach(function(post){ %>
    <%- partial('index/list.ejs', {post: post}) %>
<% }); %>
    </div>
</section>
<%- partial('index/pagination') %>
